#!/bin/bash

./RustDedicated +server.port $SERVER_PORT +server.level "$SERVER_LEVEL" +server.seed $SERVER_SEED +server.worldsize $SERVER_WORLDSIZE +server.maxplayers $SERVER_MAXPLAYERS +server.hostname $SERVER_HOSTNAME +server.description $SERVER_DESCRIPTION +server.url $SERVER_URL +server.headerimage $SERVER_HEADERIMAGE +server.identity $SERVER_IDENTITY +rcon.port $RCON_PORT +rcon.password $RCON_PASSWORD +rcon.web $RCON_WEB
